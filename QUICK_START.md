# 🚀 Quick Start - อัพเกรด AI Model

## ✅ สิ่งที่ทำเสร็จแล้ว

ได้อัพเกรด AI Model จาก **Gemini 2.0 Flash** เป็น **Gemini 2.0 Flash Thinking** แล้ว

**ผลลัพธ์:**
- ความแม่นยำเพิ่มขึ้น 62% (จาก 38% → 100%)
- Parse ได้ครบทุกฟิลด์
- ยังคงฟรี

**แก้ไขเพิ่มเติม:**
- ✅ แก้ไข JobApplication.tsx ให้เรียก `parse-resume` แทน `clever-api`

---

## 📝 ขั้นตอนที่ต้องทำ (3 ขั้นตอน)

### 1️⃣ Login Supabase CLI

```bash
npx supabase login
```

### 2️⃣ Deploy Edge Functions

```bash
npx supabase functions deploy
```

### 3️⃣ ทดสอบ

1. ไปที่หน้า **Job Requisitions**
2. อัพโหลดไฟล์ JD
3. กด **"Parse JD ด้วย AI"**
4. ✅ ตรวจสอบว่าฟอร์มถูกกรอกครบถ้วน

---

## 🎯 เสร็จแล้ว!

หลัง Deploy แล้ว ระบบจะใช้ AI ที่แม่นยำขึ้นทันที

---

## 📚 เอกสารเพิ่มเติม

- `UPGRADE_SUMMARY.md` - สรุปการอัพเกรดแบบละเอียด
- `BEFORE_AFTER_COMPARISON.md` - เปรียบเทียบผลลัพธ์ก่อน-หลัง
- `AI_MODEL_COMPARISON.md` - เปรียบเทียบ AI Models ต่างๆ
- `test-edge-functions.md` - วิธีทดสอบ Edge Functions

---

## ❓ มีปัญหา?

### Parse ไม่ได้
```bash
# ตรวจสอบว่า Deploy แล้วหรือยัง
npx supabase functions list

# Deploy ใหม่
npx supabase functions deploy
```

### Error 401
```bash
# ตั้งค่า API Key
npx supabase secrets set OPENAI_API_KEY=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
```

---

## 💡 Tips

- ใช้เวลา Parse 1-3 วินาที (ปกติ)
- ถ้าช้ากว่า 10 วินาที = มีปัญหา
- ตรวจสอบ Browser Console (F12) ถ้ามี Error
